export enum LocalStorageKeys {
    LANGUAGE = "language",
    VIBRATIONS = 'vibrations',
    REMOTE_FEDERATION_CONFIG = 'remoteFederationConfig',
    SCAN_DATA = 'scanData',
}

export enum Tabs {
    HOME = "home",
    SCAN = "scan",
    REDIRECT = "redirect",
}

export enum Languages {
    EN = "en",
    ES = "es",
    CAT = "cat",
    GL = "gl",
    EU = "eu"
}

export enum LogKeys {
    SYSTEM = "[SYSTEM]",
}